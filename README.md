# Spam_Email_detection_using-python
The project builds a supervised ML model to classify emails as spam or not. It applies preprocessing like text cleaning, tokenization, and TF-IDF/BoW for feature extraction. Models such as Naïve Bayes, Logistic Regression, and SVM are trained, achieving high accuracy in spam detection.
